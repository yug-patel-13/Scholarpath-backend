-- Create database for ScholarPath
CREATE DATABASE IF NOT EXISTS scholarpath CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Use the database
USE scholarpath;

-- The migrations will create all tables, so you just need to create the database
-- Run this SQL script in your MySQL client or phpMyAdmin

